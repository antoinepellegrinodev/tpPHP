{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ annonce.Titre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ annonce.Prix }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ annonce.Contenu }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ annonce.codePostal }}</td>
            </tr>
            <tr>
                <th>DateDeCreation</th>
                <td>{{ annonce.dateDeCreation ? annonce.dateDeCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annonce_index') }}">back to list</a>

    <a href="{{ path('app_annonce_edit', {'id': annonce.id}) }}">edit</a>

    {{ include('annonce/_delete_form.html.twig') }}
{% endblock %}
